name = "allowance-scoreboard"
main = "src/index.ts"
compatibility_date = "2024-09-23"
# Required by @cloudflare/vitest-pool-workers (the test pool refuses to start
# without it); also applied at deploy.
compatibility_flags = ["nodejs_compat"]

# Local tests simulate KV via vitest-pool-workers and ignore this id. The id
# below is the real production namespace created with `wrangler kv namespace
# create SCOREBOARD`.
[[kv_namespaces]]
binding = "SCOREBOARD"
id = "d6e027aa4f264662bf065efa7128fef5"
